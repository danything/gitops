{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "github>5ym/renovate-config"
    ],
    "kubernetes": {
        "description": "The kubernetes manager ships no default file patterns, so without this the manifests here are never scanned for image tags at all.",
        "managerFilePatterns": [
            "**/*.yaml"
        ]
    },
    "customManagers": [
        {
            "description": "k3s's HelmChart CRD isn't a format Renovate knows, so erpnext's chart version is matched by hand, off the chart/repo/version trio in the order the CRD writes them.",
            "customType": "regex",
            "managerFilePatterns": [
                "**/helmchart.yaml"
            ],
            "matchStrings": [
                "chart:\\s*(?<depName>\\S+)\\s*\\n\\s*repo:\\s*(?<registryUrl>\\S+)\\s*\\n\\s*version:\\s*(?<currentValue>\\S+)"
            ],
            "datasourceTemplate": "helm"
        },
        {
            "description": "talos/versions.yaml の `# renovate: datasource=… depName=…` の次の行にある version を見る。Talos の版は machine config にも README にも散らばるので、追う対象はこの 1 ファイルに寄せている。",
            "customType": "regex",
            "managerFilePatterns": [
                "^talos/versions\\.yaml$"
            ],
            "matchStrings": [
                "#\\s*renovate:\\s*datasource=(?<datasource>\\S+)\\s+depName=(?<depName>\\S+)\\s*\\n\\s*version:\\s*(?<currentValue>\\S+)"
            ]
        }
    ],
    "packageRules": [
        {
            "description": "Mattermost の DB。Mattermost が挙げているのは下限 (PostgreSQL 14.0+) だけで、18 を検証したとは書いていない。加えて PostgreSQL はメジャーが変わるとデータディレクトリの互換が切れるので、タグを差し替えるだけでは上がらない (dump と restore が要る)。17 のサポートは 2029-11 まであるので、Mattermost が 18 を明記するまで 17 の線に留める。",
            "matchDatasources": [
                "docker"
            ],
            "matchPackageNames": [
                "postgres"
            ],
            "allowedVersions": "<18"
        },
        {
            "description": "Talos と Kubernetes は自動マージしない。ノードが 1 台なので、Talos を上げることは唯一のノードの再起動、つまり全停止を意味する。PR は「新しい版が出た」という通知として使い、適用は talosctl upgrade で人がやる (talos/versions.yaml)。",
            "matchFileNames": [
                "talos/versions.yaml"
            ],
            "groupName": "talos",
            "groupSlug": "talos",
            "automerge": false
        }
    ]
}
