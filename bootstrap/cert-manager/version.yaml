# **cert-manager の chart のバージョン。**
#
# **2026-09-08 まで、これはどこにも無かった。** `helm install` した一度きりで入っていて、
# 版も値も git に記録が無い状態だった(cilium で 2026-09-07 に直したのと同じ形の穴)。
# **作り直すと cert-manager が入らない**か、入っても別の版になる。
#
# 追うのは renovate.json の customManagers。
# renovate: datasource=helm depName=cert-manager registryUrl=https://charts.jetstack.io
version: v1.21.1
