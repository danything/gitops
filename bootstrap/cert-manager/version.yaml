# cert-manager の chart のバージョン。経緯と当て方は values.yaml に書いてある。
# 実機で動いているものと同じ値(`helm list -n cert-manager` → `cert-manager-v1.21.1`)。
# renovate: datasource=helm depName=cert-manager registryUrl=https://charts.jetstack.io
version: v1.21.1
