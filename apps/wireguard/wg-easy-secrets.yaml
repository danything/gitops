# wg-easy の秘密は Infisical (il.doany.io) にあり、Infisical 純正 operator が Secret にする。
# 値を変えるときは Infisical の UI で書き換えるだけ (push-bridge が数秒で同期)。
#
# ここまで唯一「クラスタにしか無い」手で apply した Secret だったので、復元リハーサル
# (bootstrap/docs/restore-drill.md の 5) で復元後に失われ、wg-easy が起動できなくなった。
#
# Infisical 側に要るフォルダとキー:
#   /wireguard/wg-easy-init … INIT_PASSWORD          (管理画面の初期パスワード)
#   /wireguard/wg-easy-oidc … OAUTH_OIDC_CLIENT_ID, OAUTH_OIDC_CLIENT_SECRET
#
# wg-easy は自前の Entra アプリ登録を使っている。共用アプリ (/shared/entra) に寄せるなら、
# Entra 側に wg-easy のリダイレクト URI を足したうえで、Infisical の値を
# ${prod.shared.entra.client-id} / ${prod.shared.entra.client-secret} という参照にする
# (operator が展開するので、この CR は変えなくてよい)。
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: wg-easy-init
  namespace: wireguard
spec:
  hostAPI: http://infisical.infisical.svc:8080/api
  authentication:
    kubernetesAuth:
      identityId: f2bc3416-f196-4626-9bf3-c11693b32cc6
      autoCreateServiceAccountToken: true
      serviceAccountRef:
        name: infisical-auth
        namespace: infisical
      secretsScope:
        projectSlug: k3s-cluster
        envSlug: prod
        secretsPath: /wireguard/wg-easy-init
        recursive: false
  managedKubeSecretReferences:
    - secretName: wg-easy-init
      secretNamespace: wireguard
      # Orphan: CR を消しても Secret は残す（移行や再作成で稼働中の Pod を巻き込まない）
      creationPolicy: Orphan
---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: wg-easy-oidc
  namespace: wireguard
spec:
  hostAPI: http://infisical.infisical.svc:8080/api
  authentication:
    kubernetesAuth:
      identityId: f2bc3416-f196-4626-9bf3-c11693b32cc6
      autoCreateServiceAccountToken: true
      serviceAccountRef:
        name: infisical-auth
        namespace: infisical
      secretsScope:
        projectSlug: k3s-cluster
        envSlug: prod
        secretsPath: /wireguard/wg-easy-oidc
        recursive: false
  managedKubeSecretReferences:
    - secretName: wg-easy-oidc
      secretNamespace: wireguard
      creationPolicy: Orphan
