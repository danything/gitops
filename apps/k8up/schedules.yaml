# **k8up の Schedule を 1 か所にまとめてある。** 中身は名前と namespace と時刻しか
# 違わないので、アプリごとにファイルを分けると同じ注意書きが 9 回並ぶ。
# **何をバックアップするか**(どの Pod・どの PVC の注釈が効くか)は apps/k8up/README.md の表。
# **注釈だけでは動かない。** その namespace に Schedule が無いとジョブ自体が作られないので、
# PVC や Pod に注釈を足したら**ここに namespace があるかを必ず確かめる**。
#
# ## 決まりごと
#
# - **`SkipDryRunOnMissingResource` は必須。** k8up の CRD がまだ無いクラスタだと、
#   これが無いと `apps/` の同期が丸ごと落ちる(README「CRD がまだ無いクラスタで〜」)
# - **`tags: [k8up]` を付ける。** prune は `--host=<namespace>` で自分の namespace の
#   スナップショットしか消さない(`restic/cli/prune.go`)ので、ホストのスクリプトの
#   ぶん(`host=main` / `tag=k3s-host`)には元から届かない。タグは二重の歯止め
# - **prune は namespace ごとに要る。** 上と同じ理由で、1 本にまとめても他の
#   namespace には効かない
# - **時刻はホストのスクリプト(19:00 UTC = 04:00 JST)を避ける。** restic は
#   リポジトリをロックするので、重なると片方が待たされる。
#   バックアップは 15:00〜17:30 UTC、prune は日曜の 20:00〜22:30 UTC に置いてある
# - 保持は全部 `keepDaily 7 / keepWeekly 4 / keepMonthly 6`
# - **バックエンドは書かない。** 書くと R2 のエンドポイント(Cloudflare の
#   アカウント ID を含む)が git に載る。operator のグローバル設定に寄せてある
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: mattermost
  namespace: mattermost
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "0 15 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "0 20 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: erpnext
  namespace: erpnext
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "15 15 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "15 20 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: infisical
  namespace: infisical
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "30 15 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "30 20 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: lgtm
  namespace: lgtm
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "45 15 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "45 20 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: xool
  namespace: xool
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "0 16 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "0 21 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: worklog
  namespace: worklog
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "15 16 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "15 21 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: denpa
  namespace: denpa
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "30 16 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "30 21 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: yosegaki
  namespace: blog
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "45 16 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "45 21 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: netbird
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "0 17 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "0 22 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: adguardhome
  namespace: adguardhome
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "15 17 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "15 22 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
---
apiVersion: k8up.io/v1
kind: Schedule
metadata:
  name: portainer
  namespace: portainer
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  backup:
    schedule: "30 17 * * *"
    tags: [k8up]
    failedJobsHistoryLimit: 2
    successfulJobsHistoryLimit: 1
  prune:
    schedule: "30 22 * * 0"
    retention:
      keepDaily: 7
      keepWeekly: 4
      keepMonthly: 6
      tags: [k8up]
