apiVersion: v1
kind: Service
metadata:
  name: 3proxy
  namespace: 3proxy
spec:
  selector:
    app: 3proxy
  ports:
    - name: proxy
      port: 3128
      targetPort: 3128
    # Gateway からの TLS passthrough はこちらに入る(サイドカーが終端する)。
    - name: proxy-tls
      port: 3129
      targetPort: 3129
